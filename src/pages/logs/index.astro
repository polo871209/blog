---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { titleOf, byDateDesc, iso } from "../../lib/posts";

const posts = (await getCollection("logs")).sort(byDateDesc);
---
<BaseLayout title="Logs" section="logs">
  <section class="list">
    <div class="prose lede"><p>Just sharing the simple solutions that work.</p></div>
    <ul class="post-list">
      {posts.map((p) => (
        <li>
          <a href={`/logs/${p.id}/`}>
            {p.data.date && <time datetime={iso(p.data.date)}>{iso(p.data.date)}</time>}
            <span class="post-title">{titleOf(p)}</span>
          </a>
        </li>
      ))}
    </ul>
  </section>
</BaseLayout>
